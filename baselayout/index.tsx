import BaseLayout from "../../components/layout";

function ShowDashboard() {
    const passADiv = <div>Pass any div here, you must pass it!</div>;
    return (
        <BaseLayout>{passADiv}</BaseLayout>
    );
}

export default ShowDashboard;